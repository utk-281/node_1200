const { v2 } = require("../config/cloudinary");
const asyncHandler = require("express-async-handler");
console.log(v2);

const uploadImgOnCloudinary = asyncHandler((path) => {
  // let uploadedResponse = await v2
});
